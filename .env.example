# ws-gateway 의 y-websocket 엔드포인트 (room 은 코드에서 append)
VITE_WS_URL=ws://localhost:8080/ws/doc

# doc-service REST 베이스 URL — 로그인·페이지 API. CORS 화이트리스트에 이 앱의 origin
# (http://localhost:5173)이 이미 등록돼 있다(doc-service SecurityConfig).
VITE_API_URL=http://localhost:8081
